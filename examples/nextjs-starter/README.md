# Next.js Starter Example

This directory is a placeholder for a starter example using tokiui.

To create it:

```bash
pnpm create next-app examples/nextjs-starter --typescript --tailwind --app
cd examples/nextjs-starter
npx @tokiui/cli init
npx @tokiui/cli add button card input badge dialog
```
