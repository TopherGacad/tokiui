export const metadata = { title: 'Breadcrumb — tokiui' }

import { PageHeader } from '@/components/docs/page-header'
import { TerminalInstall } from '@/components/docs/terminal-install'
import { ManualInstall } from '@/components/docs/manual-install'
import { Showcase } from '@/components/docs/showcase'
import {
  BreadcrumbBasicPreview,
  BreadcrumbCustomSeparatorPreview,
  BreadcrumbEllipsisPreview,
  BreadcrumbSettingsPreview,
  BreadcrumbProductPreview,
} from '@/components/docs/breadcrumb-demo'

<PageHeader
  title="Breadcrumb"
  description="A navigation trail showing the user's location in a hierarchy. Fully composable — add custom separators, collapse long paths with an ellipsis, and drop in any routing library link."
  version="v0.3.0"
  sourceHref="https://github.com/TopherGacad/tokiui"
/>

## Installation

<TerminalInstall component="breadcrumb" />
<ManualInstall component="breadcrumb" />

## Import

```tsx
import {
  Breadcrumb,
  BreadcrumbList,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbPage,
  BreadcrumbSeparator,
  BreadcrumbEllipsis,
} from '@tokiui/ui'
```

## Basic

`BreadcrumbPage` marks the current location — it renders as a non-interactive span with `aria-current="page"`.

<Showcase title="Basic" code={`<Breadcrumb>\n  <BreadcrumbList>\n    <BreadcrumbItem>\n      <BreadcrumbLink href="/home">Home</BreadcrumbLink>\n    </BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem>\n      <BreadcrumbLink href="/components">Components</BreadcrumbLink>\n    </BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem>\n      <BreadcrumbPage>Breadcrumb</BreadcrumbPage>\n    </BreadcrumbItem>\n  </BreadcrumbList>\n</Breadcrumb>`}>
  <BreadcrumbBasicPreview />
</Showcase>

## Custom separator

Pass any character or element as children to `BreadcrumbSeparator`. The default is a chevron-right icon.

<Showcase title="Custom separator" code={`{/* slash */}\n<BreadcrumbSeparator>/</BreadcrumbSeparator>\n\n{/* dot */}\n<BreadcrumbSeparator>·</BreadcrumbSeparator>`}>
  <BreadcrumbCustomSeparatorPreview />
</Showcase>

## Collapsed with ellipsis

Use `BreadcrumbEllipsis` to represent hidden intermediate segments in a long path. Wire it to a dropdown or tooltip for a fully accessible collapsed breadcrumb.

<Showcase title="Ellipsis" code={`<Breadcrumb>\n  <BreadcrumbList>\n    <BreadcrumbItem>\n      <BreadcrumbLink href="#">Home</BreadcrumbLink>\n    </BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem>\n      <BreadcrumbEllipsis />\n    </BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem>\n      <BreadcrumbLink href="#">Components</BreadcrumbLink>\n    </BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem>\n      <BreadcrumbPage>Breadcrumb</BreadcrumbPage>\n    </BreadcrumbItem>\n  </BreadcrumbList>\n</Breadcrumb>`}>
  <BreadcrumbEllipsisPreview />
</Showcase>

## Settings page

<Showcase title="Settings" code={`<Breadcrumb>\n  <BreadcrumbList>\n    <BreadcrumbItem><BreadcrumbLink href="#">Dashboard</BreadcrumbLink></BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem><BreadcrumbLink href="#">Account</BreadcrumbLink></BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem><BreadcrumbPage>Notifications</BreadcrumbPage></BreadcrumbItem>\n  </BreadcrumbList>\n</Breadcrumb>`}>
  <BreadcrumbSettingsPreview />
</Showcase>

## Product page

<Showcase title="Product" code={`<Breadcrumb>\n  <BreadcrumbList>\n    <BreadcrumbItem><BreadcrumbLink href="#">Shop</BreadcrumbLink></BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem><BreadcrumbLink href="#">Electronics</BreadcrumbLink></BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem><BreadcrumbLink href="#">Laptops</BreadcrumbLink></BreadcrumbItem>\n    <BreadcrumbSeparator />\n    <BreadcrumbItem><BreadcrumbPage>Studio Pro 15</BreadcrumbPage></BreadcrumbItem>\n  </BreadcrumbList>\n</Breadcrumb>`}>
  <BreadcrumbProductPreview />
</Showcase>

## Routing

`BreadcrumbLink` renders a plain `<a>` by default. Pass `asChild` to swap it for any routing library's `<Link>` component — the breadcrumb keeps its styles, the link handles navigation.

### Next.js App Router

Wrap every non-current item in `BreadcrumbLink asChild` + `Link`. The last item is always `BreadcrumbPage` (no link).

```tsx
import Link from 'next/link'
import {
  Breadcrumb, BreadcrumbList, BreadcrumbItem,
  BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator,
} from '@tokiui/ui'

export function AppBreadcrumb() {
  return (
    <Breadcrumb>
      <BreadcrumbList>
        <BreadcrumbItem>
          <BreadcrumbLink asChild>
            <Link href="/">Home</Link>
          </BreadcrumbLink>
        </BreadcrumbItem>
        <BreadcrumbSeparator />
        <BreadcrumbItem>
          <BreadcrumbLink asChild>
            <Link href="/docs/components">Components</Link>
          </BreadcrumbLink>
        </BreadcrumbItem>
        <BreadcrumbSeparator />
        <BreadcrumbItem>
          <BreadcrumbPage>Breadcrumb</BreadcrumbPage>
        </BreadcrumbItem>
      </BreadcrumbList>
    </Breadcrumb>
  )
}
```

### Dynamic breadcrumb from current path (Next.js)

Use `usePathname()` to generate the trail automatically from the URL. Each URL segment becomes one breadcrumb item — all are links except the last, which becomes `BreadcrumbPage`.

```tsx
'use client'

import * as React from 'react'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import {
  Breadcrumb, BreadcrumbList, BreadcrumbItem,
  BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator,
} from '@tokiui/ui'

function toLabel(segment: string) {
  return segment.replace(/-/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase())
}

export function DynamicBreadcrumb() {
  const pathname  = usePathname()
  const segments  = pathname.split('/').filter(Boolean)

  return (
    <Breadcrumb>
      <BreadcrumbList>
        <BreadcrumbItem>
          <BreadcrumbLink asChild>
            <Link href="/">Home</Link>
          </BreadcrumbLink>
        </BreadcrumbItem>
        {segments.map((segment, index) => {
          const href   = '/' + segments.slice(0, index + 1).join('/')
          const isLast = index === segments.length - 1
          return (
            <React.Fragment key={href}>
              <BreadcrumbSeparator />
              <BreadcrumbItem>
                {isLast ? (
                  <BreadcrumbPage>{toLabel(segment)}</BreadcrumbPage>
                ) : (
                  <BreadcrumbLink asChild>
                    <Link href={href}>{toLabel(segment)}</Link>
                  </BreadcrumbLink>
                )}
              </BreadcrumbItem>
            </React.Fragment>
          )
        })}
      </BreadcrumbList>
    </Breadcrumb>
  )
}
```

For routes with dynamic segments (e.g. `/products/[id]`), replace the raw segment with a resolved label — fetch the product name from your data layer and pass it instead of calling `toLabel(segment)`.

### React Router / Remix

```tsx
import { Link } from 'react-router-dom'  // or '@remix-run/react'

<BreadcrumbLink asChild>
  <Link to="/products">Products</Link>
</BreadcrumbLink>
```

### TanStack Router

```tsx
import { Link } from '@tanstack/react-router'

<BreadcrumbLink asChild>
  <Link to="/products">Products</Link>
</BreadcrumbLink>
```

## API

### Breadcrumb

Renders a `<nav aria-label="breadcrumb">`. All props forwarded to `<nav>`.

### BreadcrumbList

Renders `<ol>`. Handles flex layout and gap.

### BreadcrumbItem

Renders `<li>`. Wrap each link, page, or ellipsis in one.

### BreadcrumbLink

| Prop | Type | Default | Description |
|---|---|---|---|
| `asChild` | `boolean` | `false` | Replace `<a>` with the child element (e.g. `<Link>` from Next.js) |
| `href` | `string` | — | Standard anchor href |

### BreadcrumbPage

Renders the current page label. Sets `aria-current="page"` and `aria-disabled="true"`. No `href`.

### BreadcrumbSeparator

Renders between items. Defaults to a chevron-right icon. Pass children to use a custom separator.

### BreadcrumbEllipsis

Renders a `···` indicator with a `sr-only` label `"More pages"` for screen readers.

## Accessibility

- The `<nav>` wrapper carries `aria-label="breadcrumb"` per WAI-ARIA
- `BreadcrumbPage` is marked `aria-current="page"` so screen readers announce the current location
- `BreadcrumbSeparator` and `BreadcrumbEllipsis` are both `aria-hidden="true"` — they are decorative and not announced
- The `<ol>` list communicates the number of items to screen readers automatically
