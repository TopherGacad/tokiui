---
"@tokiui/ui": minor
---

Add **Chart** — zero-dependency SVG charts: `Sparkline`, `AreaChart` (interactive crosshair + value tooltip), `BarChart`, `DonutChart`, and `RadialChart`. Data-driven and themed with your design tokens (they retint with light/dark). No charting dependency.
