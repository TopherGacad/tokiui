---
"@tokiui/ui": minor
---

Add **Table** — composable, accessible table primitives (`Table`, `TableHeader`, `TableBody`, `TableFooter`, `TableRow`, `TableHead`, `TableCell`, `TableCaption`). Use them directly for static tables, or compose with state for sortable / paginated / selectable data tables. No new dependencies.
